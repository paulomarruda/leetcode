#pragma once 

#ifndef BINARY_SEARCH_H
#define BINARY_SEARCH_H

int binary_search(int arr[], int size_arr, int target);
/*
 * Detects sign change in a sorted array
 * @brief Detects sign change between the begining of an 
 * array of integers and 
 * @param arr
 * @param range_max
 */
int argSignChange(int arr[], int range_max);
#endif
